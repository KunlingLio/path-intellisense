# Path Intellisense

## Development
### 1. Build `path-server`
```bash
cd path-server
cargo build
```
### 2. Install dev extension
Open command panel and input "zed: install dev extension", then select `path-intellisense` and install it.
